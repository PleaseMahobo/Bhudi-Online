{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "/bin/sh -c \"exec python -u -m uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level debug --ws websockets --ws-max-size 1048576 --ws-max-queue 16 --ws-ping-interval 20 --ws-ping-timeout 20 --ws-per-message-deflate true\"",
    "healthcheckPath": "/health"
  }
}
