# Bhudi Support Client

Native Windows system-tray support client for the Bhudi endpoint agent.

The client is intentionally kept on the loopback interface and authenticates remote ticket operations with the enrolled agent identity.
