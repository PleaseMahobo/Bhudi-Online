'use client';

import { useCallback, useEffect, useRef, useState } from 'react';
import { useSearchParams } from 'next/navigation';
import { Loader2, Monitor, RefreshCw, Terminal, Wifi, XCircle } from 'lucide-react';
import { getDevices } from '@/lib/api';

type Device = { id: string; device_id?: string; agent_id?: string; hostname?: string; name?: string; status?: string };
type MonitorInfo = { index: number; name: string; width: number; height: number; primary?: boolean };
const API_BASE = '';

function authHeaders(): HeadersInit { return { 'Content-Type': 'application/json', Accept: 'application/json' }; }
function wsBase(): string { if (typeof window === 'undefined') return ''; return window.location.origin.replace(/^http/, 'ws'); }

export default function RemoteAccessConsole() {
  const searchParams = useSearchParams(); const [devices, setDevices] = useState<Device[]>([]); const [agentId, setAgentId] = useState(''); const [mode, setMode] = useState<'desktop' | 'terminal'>('desktop'); const [shell, setShell] = useState('powershell'); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const [sessionId, setSessionId] = useState<string | null>(null); const [status, setStatus] = useState('Idle'); const [termLog, setTermLog] = useState(''); const [termInput, setTermInput] = useState(''); const [hasFrame, setHasFrame] = useState(false); const [monitors, setMonitors] = useState<MonitorInfo[]>([]); const [monitorIndex, setMonitorIndex] = useState(0); const [fitMode, setFitMode] = useState<'contain' | 'width'>('contain');
  const wsRef = useRef<WebSocket | null>(null); const canvasRef = useRef<HTMLCanvasElement | null>(null); const frameSize = useRef({ w: 1280, h: 720 });
  const loadDevices = useCallback(async () => { try { const rows = await getDevices().catch(() => []); setDevices(Array.isArray(rows) ? rows as Device[] : []); } catch { setDevices([]); } }, []);
  useEffect(() => { void loadDevices(); const id = setInterval(loadDevices, 15000); return () => clearInterval(id); }, [loadDevices]);
  useEffect(() => { const a = searchParams.get('agent') || searchParams.get('device') || ''; const m = searchParams.get('mode'); if (a) setAgentId(a); if (m === 'desktop' || m === 'terminal') setMode(m); }, [searchParams]);
  useEffect(() => () => { wsRef.current?.close(); }, []);
  function deviceAgentId(d: Device): string { return String(d.agent_id || d.device_id || d.id || ''); }
  function sendInput(payload: Record<string, unknown>) { const ws = wsRef.current; if (!ws || ws.readyState !== WebSocket.OPEN) return; ws.send(JSON.stringify(payload)); }
  function canvasCoords(e: React.MouseEvent<HTMLCanvasElement>) { const canvas = canvasRef.current; if (!canvas) return { x: 0, y: 0 }; const rect = canvas.getBoundingClientRect(); const rw = Math.max(rect.width, 1); const rh = Math.max(rect.height, 1); const x = Math.round(((e.clientX - rect.left) / rw) * frameSize.current.w); const y = Math.round(((e.clientY - rect.top) / rh) * frameSize.current.h); return { x: Math.max(0, Math.min(frameSize.current.w - 1, x)), y: Math.max(0, Math.min(frameSize.current.h - 1, y)) }; }
  function onCanvasMouseDown(e: React.MouseEvent<HTMLCanvasElement>) { e.preventDefault(); canvasRef.current?.focus(); const { x, y } = canvasCoords(e); sendInput({ type: 'mousedown', x, y, button: e.button === 2 ? 'right' : e.button === 1 ? 'middle' : 'left' }); }
  function onCanvasMouseUp(e: React.MouseEvent<HTMLCanvasElement>) { e.preventDefault(); const { x, y } = canvasCoords(e); sendInput({ type: 'mouseup', x, y, button: e.button === 2 ? 'right' : e.button === 1 ? 'middle' : 'left' }); }
  function onCanvasClick(e: React.MouseEvent<HTMLCanvasElement>) { e.preventDefault(); const { x, y } = canvasCoords(e); sendInput({ type: 'click', x, y, button: e.button === 2 ? 'right' : e.button === 1 ? 'middle' : 'left' }); }
  function onCanvasMouseMove(e: React.MouseEvent<HTMLCanvasElement>) { if (e.buttons === 0) return; const { x, y } = canvasCoords(e); sendInput({ type: 'mousemove', x, y }); }
  function onCanvasWheel(e: React.WheelEvent<HTMLCanvasElement>) { e.preventDefault(); const { x, y } = canvasCoords(e as unknown as React.MouseEvent<HTMLCanvasElement>); sendInput({ type: 'wheel', x, y, deltaY: e.deltaY > 0 ? 120 : -120 }); }
  function onCanvasKeyDown(e: React.KeyboardEvent<HTMLCanvasElement>) { e.preventDefault(); sendInput({ type: 'keydown', key: e.key }); }
  function onCanvasKeyUp(e: React.KeyboardEvent<HTMLCanvasElement>) { sendInput({ type: 'keyup', key: e.key }); }
  async function startSession() {
    if (!agentId) { setError('Select a device / agent first.'); return; } setBusy(true); setError(''); setStatus('Queueing session…'); setHasFrame(false); setTermLog(''); wsRef.current?.close();
    try { const path = mode === 'desktop' ? '/api/v1/runtime/remote/desktop' : '/api/v1/runtime/remote/terminal'; const body = mode === 'desktop' ? { agent_id: agentId, session_mode: 'control', display_protocol: 'native', monitor_index: monitorIndex } : { agent_id: agentId, shell }; const res = await fetch(API_BASE + path, { method: 'POST', headers: authHeaders(), credentials: 'include', cache: 'no-store', body: JSON.stringify(body) }); const data = await res.json().catch(() => ({})); if (!res.ok) throw new Error(data.detail || res.statusText || 'Failed to start'); const sid = data.session_id as string; if (!sid) throw new Error('No session_id returned'); setSessionId(sid); setStatus('Connecting…'); const url = wsBase() + '/api/v1/remote-access/sessions/' + sid + '/dashboard'; const ws = new WebSocket(url); wsRef.current = ws; ws.onopen = () => setStatus('Connected — waiting for agent'); ws.onerror = () => setError('WebSocket error'); ws.onclose = () => setStatus('Disconnected'); ws.onmessage = (ev) => { try { const msg = JSON.parse(String(ev.data)); const type = msg.type as string; if (type === 'desktop_ready') { setStatus('Desktop ready' + (msg.monitor_index !== undefined ? ' · display ' + msg.monitor_index : '')); if (msg.width && msg.height) frameSize.current = { w: Number(msg.width), h: Number(msg.height) }; if (Array.isArray(msg.monitors) && msg.monitors.length) setMonitors(msg.monitors as MonitorInfo[]); } if (type === 'frame' && msg.data) { const img = new Image(); img.onload = () => { const c = canvasRef.current; if (!c) return; const ctx = c.getContext('2d'); if (!ctx) return; c.width = img.width; c.height = img.height; frameSize.current = { w: img.width, h: img.height }; ctx.drawImage(img, 0, 0); setHasFrame(true); setStatus('Streaming'); }; img.src = 'data:image/jpeg;base64,' + msg.data; } if (type === 'terminal_output' || type === 'output') setTermLog((prev) => prev + (msg.data || msg.output || '')); if (type === 'error') setError(String(msg.message || 'Session error')); if (type === 'session_closed') setStatus('Session ended'); } catch {} }; } catch (e) { setError(e instanceof Error ? e.message : 'Failed to start session'); setStatus('Idle'); } finally { setBusy(false); }
  }
  function stopSession() { sendInput({ type: 'close' }); wsRef.current?.close(); wsRef.current = null; setSessionId(null); setStatus('Idle'); setHasFrame(false); }
  function sendTerm() { if (!wsRef.current || wsRef.current.readyState !== WebSocket.OPEN || !termInput.trim()) return; wsRef.current.send(JSON.stringify({ type: 'command', command: termInput })); setTermInput(''); }
  const monitorOptions: MonitorInfo[] = monitors.length > 0 ? monitors : [{ index: 0, name: 'Primary / Display 1', width: 0, height: 0, primary: true }, { index: 1, name: 'Display 2', width: 0, height: 0 }, { index: 2, name: 'Display 3', width: 0, height: 0 }];
  return <div className="space-y-4"><div className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex flex-wrap items-end gap-3"><div className="min-w-[200px] flex-1"><label className="mb-1 block text-xs font-medium text-slate-500">Device / agent</label><select value={agentId} onChange={(e) => setAgentId(e.target.value)} className="w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 text-sm"><option value="">Select device…</option>{devices.map((d) => { const id = deviceAgentId(d); return <option key={id} value={id}>{(d.hostname || d.name || id) + (d.status ? ' (' + d.status + ')' : '')}</option>; })}</select></div><div className="flex gap-1 rounded-xl border border-slate-200 p-1"><button type="button" onClick={() => setMode('desktop')} className={'inline-flex items-center gap-1 rounded-lg px-3 py-1.5 text-xs font-medium ' + (mode === 'desktop' ? 'bg-indigo-600 text-white' : 'text-slate-600')}><Monitor size={14} /> Desktop</button><button type="button" onClick={() => setMode('terminal')} className={'inline-flex items-center gap-1 rounded-lg px-3 py-1.5 text-xs font-medium ' + (mode === 'terminal' ? 'bg-indigo-600 text-white' : 'text-slate-600')}><Terminal size={14} /> Terminal</button></div>{mode === 'desktop' && <div className="min-w-[180px]"><label className="mb-1 block text-xs font-medium text-slate-500">Display</label><select value={monitorIndex} onChange={(e) => setMonitorIndex(Number(e.target.value))} className="w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 text-sm">{monitorOptions.map((m) => <option key={m.index} value={m.index}>{m.name}{m.width ? ` (${m.width}×${m.height})` : ''}{m.primary ? ' ★' : ''}</option>)}</select><p className="mt-1 text-[11px] text-slate-400">Change display, then Connect again.</p></div>}{mode === 'terminal' && <select value={shell} onChange={(e) => setShell(e.target.value)} className="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="powershell">PowerShell</option><option value="cmd">CMD</option><option value="bash">Bash</option></select>}{!sessionId ? <button type="button" disabled={busy || !agentId} onClick={() => void startSession()} className="inline-flex items-center gap-2 rounded-xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white hover:bg-indigo-500 disabled:opacity-50">{busy ? <Loader2 size={16} className="animate-spin" /> : null}Connect</button> : <button type="button" onClick={stopSession} className="rounded-xl border border-slate-200 px-4 py-2 text-sm font-medium text-slate-700">Disconnect</button>}<button type="button" onClick={() => void loadDevices()} className="rounded-xl border border-slate-200 p-2 text-slate-500 hover:bg-slate-50" title="Refresh devices"><RefreshCw size={16} /></button></div><div className="mt-3 flex flex-wrap items-center gap-2 text-xs text-slate-500"><Wifi size={14} /><span>Status: <span className="font-medium text-slate-800">{status}</span></span>{sessionId && <span className="font-mono text-slate-400">session {sessionId.slice(0, 8)}…</span>}</div>{error && <div className="mt-3 flex items-start gap-2 rounded-lg bg-red-50 px-3 py-2 text-sm text-red-700"><XCircle size={16} className="mt-0.5 shrink-0" /><span>{error}</span></div>}</div>{mode === 'desktop' && <div className="overflow-hidden rounded-2xl border border-slate-200 bg-slate-900 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-2 border-b border-white/10 px-3 py-2 text-xs text-slate-300"><span>Remote desktop</span><div className="flex items-center gap-2"><button type="button" onClick={() => setFitMode('contain')} className={'rounded px-2 py-1 ' + (fitMode === 'contain' ? 'bg-white/15 text-white' : 'hover:bg-white/10')}>Fit page</button><button type="button" onClick={() => setFitMode('width')} className={'rounded px-2 py-1 ' + (fitMode === 'width' ? 'bg-white/15 text-white' : 'hover:bg-white/10')}>Fit width</button><button type="button" onClick={() => { const el = canvasRef.current?.parentElement; if (el?.requestFullscreen) void el.requestFullscreen(); }} className="rounded px-2 py-1 hover:bg-white/10">Fullscreen</button></div></div><div className={'relative flex bg-black p-2 ' + (fitMode === 'contain' ? 'max-h-[min(80vh,900px)] items-center justify-center overflow-auto' : 'items-start justify-center overflow-auto')}><canvas ref={canvasRef} tabIndex={0} onMouseDown={onCanvasMouseDown} onMouseUp={onCanvasMouseUp} onClick={onCanvasClick} onMouseMove={onCanvasMouseMove} onWheel={onCanvasWheel} onKeyDown={onCanvasKeyDown} onKeyUp={onCanvasKeyUp} onContextMenu={(e) => e.preventDefault()} style={{ display: 'block', width: fitMode === 'width' ? '100%' : 'auto', maxWidth: '100%', maxHeight: fitMode === 'contain' ? 'min(80vh, 880px)' : undefined, height: 'auto', cursor: 'crosshair' }} className="outline-none focus:ring-2 focus:ring-indigo-500" />{!hasFrame && <p className="pointer-events-none absolute text-sm text-slate-500">No frames yet — Connect with agent v2.2.8+.</p>}</div><p className="border-t border-white/10 px-3 py-1.5 text-[11px] text-slate-500">Click the screen to focus, then click/drag/type. Change Display and reconnect to switch monitors.</p></div>}{mode === 'terminal' && <div className="overflow-hidden rounded-2xl border border-slate-200 bg-slate-950 shadow-sm"><pre className="max-h-[420px] min-h-[280px] overflow-auto p-4 font-mono text-[12px] text-emerald-300">{termLog || 'Terminal output will appear here…'}</pre><div className="flex border-t border-white/10"><input value={termInput} onChange={(e) => setTermInput(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && sendTerm()} placeholder="Type a command…" className="flex-1 bg-transparent px-3 py-2 font-mono text-sm text-white outline-none" disabled={!sessionId} /><button type="button" onClick={sendTerm} disabled={!sessionId} className="px-4 text-sm font-medium text-indigo-300 hover:text-white disabled:opacity-40">Send</button></div></div>}</div>;
}
